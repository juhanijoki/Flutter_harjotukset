import 'package:flutter/material.dart';

const tekstiTyyli = TextStyle(
  fontFamily: 'Vietnam',
  fontSize: 18,
);

const paksuTyyli =
    TextStyle(fontFamily: 'Vietnam', fontSize: 20, fontWeight: FontWeight.bold);

const titleTekstiTyyli =
    TextStyle(fontFamily: 'Vietnam', fontSize: 40, fontWeight: FontWeight.bold);

final appBar = AppBar(
  title: const Icon(Icons.leaderboard_outlined),
);

const korttiTekstiTyyli =
    TextStyle(fontFamily: 'Vietnam', fontSize: 18, fontWeight: FontWeight.w700);
